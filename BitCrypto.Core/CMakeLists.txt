add_library(BitCrypto.Core STATIC src/unity.cpp)
target_include_directories(BitCrypto.Core PUBLIC ${CMAKE_CURRENT_SOURCE_DIR}/include)
target_link_libraries(BitCrypto.Core PRIVATE bitcrypto_common)
if (MSVC)
  target_compile_definitions(BitCrypto.Core PRIVATE _CRT_SECURE_NO_WARNINGS)
endif()
